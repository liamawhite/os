;;; ui.el --- UI module loader -*- lexical-binding: t -*-

;; Load UI packages
(require 'custom-modeline)
(require 'dirnav)
(require 'which-key)

;; Enable custom modeline
(custom-modeline-mode 1)

(provide 'ui)
;;; ui.el ends here
