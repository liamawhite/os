# git-crypt configuration
# All files in context directories are automatically encrypted on commit

# Encrypt all files in personal context (used by personal machines)
personal/** filter=git-crypt diff=git-crypt

# Encrypt all files in docusign context (used by work machine)
docusign/** filter=git-crypt diff=git-crypt

# Encrypt all files in shared context (used by all machines)
shared/** filter=git-crypt diff=git-crypt

# Don't encrypt this configuration file itself
.gitattributes !filter !diff

# Don't encrypt README
README.md !filter !diff
